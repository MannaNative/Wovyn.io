[project]
name = "wovyn"
version = "0.1.0"
description = "L4 runs 32k at A100 speed - 0.000000 diff vs SDPA"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
dependencies = ["torch>=2.0", "triton>=2.1"]
authors = [{name="Manna Native LLC", email="tightknit@wovynio.net"}]
[project.urls]
Homepage = "https://github.com/MannaNative/wovyn"
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
